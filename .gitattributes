# Preserve exact upstream legal bytes, including line endings and significant
# source whitespace. This keeps Windows checkout conversion from invalidating
# the recorded upstream hashes.
licenses/** -text -whitespace
